import React from 'react';

// StatsPanel: Shows MIDI accuracy, timing, stats
// JP: スタッツパネル（MIDI精度・タイミングなど）

const StatsPanel: React.FC = () => {
  // TODO: Show stats/accuracy from MIDI input
  // JP: ここでMIDI入力の精度やタイミングなどの統計を表示する
  return (
    <div className="bg-gray-800 rounded-lg p-2 text-gray-300">
      <span>Stats Panel (MIDI accuracy, timing)｜統計パネル（精度・タイミング）</span>
    </div>
  );
};

export default StatsPanel;
